"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Entry = exports.CacheBase = void 0;
const events_1 = require("events");
const CacheStats_1 = require("../CacheStats");
const CacheEvents_1 = require("../CacheEvents");
const util_1 = require("../util");
const time_1 = require("@inventivetalent/time");
const DEFAULT_OPTIONS = {
    expireAfterAccess: 0,
    expireAfterWrite: 0,
    deleteOnExpiration: true,
    expirationInterval: time_1.Time.minutes(5),
    recordStats: true
};
/**
 * Base class for all cache implementations
 */
class CacheBase extends events_1.EventEmitter {
    constructor(options) {
        super({});
        this.data = new Map();
        this._stats = new CacheStats_1.CacheStats();
        this._options = Object.assign(Object.assign({}, DEFAULT_OPTIONS), options);
        // Start cleanup task if enabled
        this.runCleanup();
        CacheEvents_1.CacheEvents.forward(this._stats, this);
    }
    get options() {
        return this._options;
    }
    get stats() {
        return this._stats;
    }
    runCleanup() {
        if (this.options.deleteOnExpiration) { // don't do anything if entries shouldn't be deleted
            this.deleteExpiredEntries();
            if (this.options.expirationInterval > 0) {
                this._cleanupTimeout = setTimeout(() => this.runCleanup(), this.options.expirationInterval);
            }
        }
    }
    stopCleanupTimer() {
        clearTimeout(this._cleanupTimeout);
    }
    deleteExpiredEntries(recordStats = this.options.recordStats) {
        const toDelete = [];
        this.data.forEach(entry => {
            if (entry.isExpired(this.options)) {
                toDelete.push(entry.getKey());
                try {
                    this.emit(CacheEvents_1.CacheEvents.EXPIRE, entry.getKey(), entry.getValue());
                }
                catch (e) {
                    console.error(e);
                }
            }
        });
        toDelete.forEach(k => this.data.delete(k));
        if (recordStats) {
            this.stats.inc(CacheStats_1.CacheStats.EXPIRE, toDelete.length);
        }
    }
    ///// GET
    /**
     * Get the raw entry without doing any checks
     */
    getEntryDirect(key) {
        return this.data.get(key);
    }
    /**
     * Get the entry after checking for expiration, or <code>undefined</code> if it doesn't exist or is expired
     */
    getEntryIfPresent(key, recordStats = this.options.recordStats) {
        const entry = this.getEntryDirect(key);
        if (!entry) {
            if (recordStats) {
                this.stats.inc(CacheStats_1.CacheStats.MISS);
            }
            return undefined;
        }
        if (entry.isExpired(this.options)) {
            if (this.options.deleteOnExpiration) {
                this.invalidateEntry(key);
                if (recordStats) {
                    this.stats.inc(CacheStats_1.CacheStats.EXPIRE);
                }
            }
            if (recordStats) {
                this.stats.inc(CacheStats_1.CacheStats.MISS);
            }
            return undefined;
        }
        if (recordStats) {
            this.stats.inc(CacheStats_1.CacheStats.HIT);
        }
        return entry;
    }
    ///// PUT
    putEntry(key, entry) {
        this.data.set(key, entry);
    }
    ///// INVALIDATE
    invalidateEntry(key) {
        return this.data.delete(key);
    }
    /////
    keys() {
        return util_1.asArray(this.data.keys());
    }
    has(key) {
        return this.data.has(key);
    }
    end() {
        this.stopCleanupTimer();
        this.data.clear();
    }
}
exports.CacheBase = CacheBase;
class Entry {
    constructor(key) {
        this.key = key;
    }
    static fromJson(key, value) {
        const entry = new Entry(key);
        entry.value = value["value"];
        entry.accessTime = value["accessTime"];
        entry.writeTime = value["writeTime"];
        return entry;
    }
    getKey() {
        this.accessTime = time_1.Time.now;
        return this.key;
    }
    getValue() {
        this.accessTime = time_1.Time.now;
        return this.value;
    }
    setValue(v) {
        this.accessTime = time_1.Time.now;
        this.writeTime = time_1.Time.now;
        return this.value = v;
    }
    isExpired(options) {
        if (options.expireAfterAccess !== 0) {
            if (time_1.Time.now - this.accessTime > options.expireAfterAccess) {
                return true;
            }
        }
        if (options.expireAfterWrite !== 0) {
            if (time_1.Time.now - this.writeTime > options.expireAfterWrite) {
                return true;
            }
        }
        return false;
    }
}
exports.Entry = Entry;
//# sourceMappingURL=CacheBase.js.map